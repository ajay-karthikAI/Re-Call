web: uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}
worker: celery -A tasks.celery_app:celery_app worker --loglevel=info -Q transcription,analysis,live_insights --concurrency=${CELERY_WORKER_CONCURRENCY:-2}
